** Bash Admin Terminal
## this script in the right use case will check if your admin and open a admin terminal if your not already in one ( HELPFUL as a one click tool ) 
@echo off
:: Check for admin permissions
net session >nul 2>&1
if %errorlevel% neq 0 (
    echo Requesting administrative privileges...
    powershell -Command "Start-Process cmd -ArgumentList '/k' -Verb RunAs"
    exit /b
)

:: If already admin
echo You are running as administrator.
cmd
